import { DatabaseManager } from '../database/databaseManager.js'

const dbm = new DatabaseManager

print(dbm.getUserPasswordById("antoniogabinete"))

export function login(userId, password){ 
        
        return true
    }