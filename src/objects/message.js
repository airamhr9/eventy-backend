export class Message{
    constructor(id, userId, username, text, time, images){
        this.id = id,
        this.userId = userId,
        this.username = username,
        this.text = text,
        this.time = time,
        this.images = images
    }
}